export const metadata = {
    title: 'Mentoroid Dashboard',
    description: 'AI-powered learning platform',
  };
  
  export default function DashboardLayout({ children }) {
    return children;
  }